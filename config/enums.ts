// Constants for user roles.
export enum ROLES {
  ADMIN = 'admin',
  USER = 'user',
  STUDENT = 'student',
  TEACHER = 'teacher'
}

export enum LIST_VIEW_MODE {
  LIST = 'list',
  GRID = 'grid'
}

export enum MATERIAL_TYPES {
  PDF = 'PDF',
  VIDEO = 'VID',
  MARKDOWN = 'HTM',
  EXERCISE = 'IOC',
  NONE = ''
}
